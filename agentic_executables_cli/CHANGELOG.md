# Changelog

## [0.1.0] - 2026-03-03

### Added

- Initial AE v2 CLI release (`ae`)
- JSON-first command surface with `--human` mode
- Generation workflow with `auto|codex|template` engine modes
- Provider-agnostic inference injection via `AeCli(inferenceClient: ...)`
- Registry, verification, evaluation, definition, and instructions commands
- Skill install/update commands using repo template
- End-to-end integration test for full CLI flow (definition/instructions/generate/verify/evaluate/registry/skill)
